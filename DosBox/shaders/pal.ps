
uniform sampler2D texUnit;
uniform sampler2D palUnit;

varying vec2 texcoord;

void main()
{
	float idx = texture2D(texUnit, texcoord).r;
	vec4 col = texture2D(palUnit, vec2(idx, 0.0));
	gl_FragColor.xyz = col.xyz;
	gl_FragColor.w = 1.0;
}
